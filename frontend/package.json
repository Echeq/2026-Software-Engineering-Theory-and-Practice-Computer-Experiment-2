{
  "name": "spmp-frontend",
  "version": "1.0.0",
  "description": "SPMP Frontend - Project and Task Management Platform",
  "scripts": {
    "build": "esbuild src/login.ts src/dashboard.ts src/projects.ts src/tasks.ts src/settings.ts src/team.ts src/i18n.ts --bundle --outdir=js --platform=browser --target=es2020 --loader:.css=empty",
    "watch": "npm run build -- --watch"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "typescript": "^5.3.3"
  }
}
